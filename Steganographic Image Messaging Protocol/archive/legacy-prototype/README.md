# Archived legacy prototype

These files are retained only as historical design material. They are not imported,
packaged, tested, or executed by SIMP. Former `.bat`, `.vbs`, and `.py` files have
an additional `.txt` suffix so this archive cannot be launched accidentally. Do
not remove that suffix. The supported implementation lives in `simp/` and only
embeds inert bytes in PNG pixel data.
